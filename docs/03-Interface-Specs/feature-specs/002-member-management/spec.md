# Feature Specification: User, Staff, PT & Member Management

**Feature Branch**: `002-member-management`
**Created**: 2026-05-30
**Status**: Implemented (spec đồng bộ theo code 2026-07-15)
**Spec style**: SDD + Spec Kit — 9 components, EARS notation
**Source**: srs-use-cases (UC-03, UC-03A, UC-04, UC-05), requirements (FR-MEM, FR-USR)

> EARS legend như spec 001. Mọi path đều dưới `/api/v1`.

---

## 1. Context & Goal
Quản lý vòng đời hồ sơ người dùng: Admin quản lý tài khoản Staff/PT/Member; Admin/Staff quản lý hồ sơ hội viên; mỗi người tự cập nhật hồ sơ cá nhân của mình (self-service) + đổi ảnh đại diện. Là dữ liệu gốc cho membership, check-in, training. Mục tiêu: CRUD + tìm kiếm nhanh, dữ liệu không trùng, soft-delete bảo toàn lịch sử, **role gán khi tạo là bất biến**.

## 2. Actors
| Actor | Vai trò |
|---|---|
| Admin | CRUD tài khoản mọi role (Staff/PT/Member); quản lý hồ sơ PT; đổi trạng thái/reset mật khẩu |
| Staff | Tạo/sửa/tìm kiếm hồ sơ Member; tự cập nhật hồ sơ cá nhân |
| PT | Tự cập nhật hồ sơ cá nhân + nghề (specialty/bio); xem member được phân công (spec 005) |
| Member | Tự xem/cập nhật hồ sơ của chính mình (`/members/me`) + ảnh đại diện |

## 3. Functional Requirements (EARS)
- **FR-USR-01 (Event):** WHEN Admin tạo tài khoản với email chưa tồn tại và role hợp lệ, THE system SHALL tạo User với role chỉ định, trạng thái Active. WHERE không nhập `password`, THE system SHALL tự sinh mật khẩu tạm và trả `initialPassword` (`passwordAutoGenerated=true`); WHERE nhập `password` (≥6 ký tự), dùng mật khẩu đó. WHERE role là admin/staff và có nhập thông tin cá nhân (dob/gender/address/emergencyContact), THE system SHALL lưu vào `staff_profiles`.
- **FR-USR-02 (Unwanted):** IF email/phone đã tồn tại (chưa soft-delete), THEN 409 `DUPLICATE`.
- **FR-USR-03 (Ubiquitous):** THE system SHALL chỉ cho Admin thao tác endpoint `/users` và `/trainers` quản lý.
- **FR-USR-04 (Unwanted):** IF Admin cập nhật user với `role` khác role hiện tại, THEN THE system SHALL từ chối với 422 `ROLE_TRANSITION_NOT_ALLOWED` — **role là bất biến**, muốn đổi vai trò phải tạo tài khoản mới và khoá/xoá tài khoản cũ.
- **FR-USR-05 (Event):** WHEN Admin đổi trạng thái user (`PATCH /users/{id}/status` = active/locked) hoặc reset mật khẩu (`POST /users/{id}/reset-password`), THE system SHALL cập nhật và ghi AuditLog; reset trả `temporaryPassword`.
- **FR-MEM-01 (Event):** WHEN Admin/Staff tạo hồ sơ Member hợp lệ, THE system SHALL lưu MemberProfile liên kết User role Member. WHERE email chưa tồn tại, tạo cả User (role Member) + MemberProfile. WHERE email đã thuộc một tài khoản Member chưa có hồ sơ, THE system SHALL chỉ tạo MemberProfile gắn vào tài khoản đó và KHÔNG đổi mật khẩu (`linkedToExistingAccount=true`); WHERE tài khoản đó đã có hồ sơ hoặc email thuộc role khác, THEN 409.
- **FR-MEM-02 (Event):** WHEN Admin/Staff tìm kiếm theo tên/email/SĐT, THE system SHALL trả `PagedResult` khớp (mặc định 20/trang).
- **FR-MEM-03 (Event):** WHEN cập nhật hồ sơ Member, THE system SHALL validate (PersonValidation) và lưu, cập nhật UpdatedAt.
- **FR-MEM-04 (Ubiquitous):** THE system SHALL xóa hồ sơ theo cơ chế soft-delete (IsDeleted=1), KHÔNG xóa cứng (DATA-01).
- **FR-MEM-05 (Optional):** WHERE người gọi là Member, THE system SHALL chỉ cho xem/cập nhật hồ sơ của chính mình (ownership theo `UserId`).
- **FR-MEM-06 (Event):** WHEN Member gọi `GET/PUT /members/me` mà chưa có MemberProfile, THE system SHALL **tự tạo hồ sơ** (self-service) rồi trả về — không chặn luồng.
- **FR-PT-PROF-01 (Event):** WHEN Admin tạo PT (`POST /trainers`), THE system SHALL tạo tài khoản role PT + TrainerProfile trong **một bước** (email/mật khẩu + specialty/bio/gender/dob/kinh nghiệm), trả `initialPassword` nếu tự sinh.
- **FR-ACC-01 (Event):** WHEN người đăng nhập cập nhật hồ sơ cá nhân qua `/users/me`, THE system SHALL cho đổi tên/SĐT (`PUT /users/me`); đổi hồ sơ cá nhân theo role: Admin/Staff → `staff_profiles`, PT → `trainer_profiles`, Member được điều hướng sang `/members/me` (`NOT_SUPPORTED`).
- **FR-ACC-02 (Event):** WHEN người dùng upload ảnh đại diện (`POST /users/me/avatar`, jpeg/png/webp ≤5MB), THE system SHALL lưu qua Cloudinary và cập nhật `AvatarUrl`.

## 4. Non-functional Requirements
- **NFR-01:** Tìm kiếm < 1s với ~1000 hội viên; phân trang (mặc định 20, tối đa 100/trang).
- **NFR-02:** Mọi mutating action ghi AuditLog (`CREATE_USER`, `UPDATE_MEMBER`, `DELETE_MEMBER`, `UPDATE_AVATAR`…).
- **NFR-03:** PII (SĐT, email) không xuất hiện trong log.
- **NFR-04:** Ảnh đại diện lưu Cloudinary (URL trong `users.AvatarUrl`), DB không lưu nhị phân.

## 5. Data Model
- **member_profiles**(Id, UserId→users[1-1 UNIQUE], DateOfBirth, Gender, Address, EmergencyContact, JoinedAt, IsDeleted, CreatedAt, UpdatedAt)
- **staff_profiles**(Id, UserId→users[1-1 UNIQUE], DateOfBirth, Gender, Address, EmergencyContact, IsDeleted, CreatedAt, UpdatedAt) — cho admin/staff.
- **trainer_profiles**(Id, UserId→users[1-1 UNIQUE], Specialty, Bio, Gender, DateOfBirth, Address, EmergencyContact, YearsOfExperience, IsDeleted, CreatedAt, UpdatedAt)
- Dùng chung **users/roles/user_roles** (spec 001).
- **MemberCode** không lưu DB — suy ra dạng `MEM-{Id:D6}` khi trả response.

## 6. API Spec
| Method | Path | Role | Request | Success | Lỗi |
|---|---|---|---|---|---|
| POST | /api/v1/users | Admin | {fullName, email, phone?, role, password?, dob?, gender?, address?, emergencyContact?} | 201 (kèm initialPassword) | 400, 409 |
| GET | /api/v1/users?query=&role=&page=&pageSize= | Admin | — | 200 (paged AdminUserResponse) | 401, 403 |
| GET | /api/v1/users/{id} | Admin | — | 200 | 404 |
| PUT | /api/v1/users/{id} | Admin | {fullName?, phone?, status?, role?, dob?...} | 200 | 400, 409, 422 `ROLE_TRANSITION_NOT_ALLOWED` |
| PATCH | /api/v1/users/{id}/status | Admin | {status: active\|locked} | 200 | 400, 404 |
| POST | /api/v1/users/{id}/reset-password | Admin | — | 200 {temporaryPassword} | 404 |
| DELETE | /api/v1/users/{id} | Admin | — | 204 (soft) | 404 |
| PUT | /api/v1/users/me | tất cả (Bearer) | {fullName?, phone?} | 200 AuthUserResponse | 400, 409 |
| GET/PUT | /api/v1/users/me/profile | Admin/Staff/PT | {dob?, gender?, address?, emergencyContact?} | 200 PersonalProfileResponse | 400, 404 |
| POST | /api/v1/users/me/avatar | tất cả | multipart: file (jpeg/png/webp ≤5MB) | 200 AuthUserResponse | 400, 500/502 |
| POST | /api/v1/members | Admin, Staff | {fullName, email, phone?, password?, dob?, gender?...} | 201 CreateMemberResponse | 400, 409 |
| GET | /api/v1/members?query=&page=&pageSize= | Admin, Staff | — | 200 (paged MemberResponse) | 401, 403 |
| GET | /api/v1/members/me | Member | — | 200 MemberResponse (tự tạo hồ sơ nếu chưa có) | 401 |
| PUT | /api/v1/members/me | Member | {fullName?, phone?, dob?...} | 200 | 400, 409 |
| GET | /api/v1/members/{id} | Admin, Staff, Member(self) | — | 200 | 403, 404 |
| PUT | /api/v1/members/{id} | Admin, Staff (Member self qua /me) | {…} | 200 | 400, 404, 409 |
| DELETE | /api/v1/members/{id} | Admin | — | 204 (soft) | 404 |
| POST | /api/v1/trainers | Admin | {fullName, email, phone?, password?, specialty?, bio?, gender?, dob?, yearsOfExperience?} | 201 CreateTrainerResponse (kèm initialPassword) | 400, 409 |
| GET | /api/v1/trainers?query=&page= | Admin | — | 200 (paged TrainerResponse) | 401, 403 |
| GET | /api/v1/trainers/me | PT | — | 200 | 404 |
| GET | /api/v1/trainers/{id} | Admin | — | 200 | 404 |
| PUT | /api/v1/trainers/{id} | Admin | {specialty?, bio?, gender?, dob?, yearsOfExperience?, address?, emergencyContact?} | 200 | 400, 404 |

> **MemberResponse:** `{ id, userId, memberCode, email, fullName, avatarUrl, phone, dateOfBirth, gender, address, emergencyContact, joinedAt, status, createdAt }`.
> **Member 360 / giáo án / ghi chú self-service** (`/members/me/profile-360`, `/members/me/workout-plans`, `/members/me/notes`) — xem spec 005/006.

## 7. Error Handling (EARS Unwanted)
- IF dữ liệu không hợp lệ (email sai, thiếu field, PersonValidation lỗi), THEN 400 `VALIDATION_ERROR`.
- IF email/phone trùng, THEN 409 `DUPLICATE`.
- IF id không tồn tại/đã soft-delete, THEN 404 `NOT_FOUND`.
- IF Member truy cập hồ sơ người khác, THEN 403 `FORBIDDEN`.
- IF đổi role qua `PUT /users/{id}`, THEN 422 `ROLE_TRANSITION_NOT_ALLOWED`.
- IF sửa hồ sơ cá nhân của Member/PT qua `PUT /users/{id}` (thay vì màn tương ứng), THEN 422 `VALIDATION_ERROR`.
- IF Cloudinary chưa cấu hình khi upload avatar, THEN 500 `CLOUDINARY_NOT_CONFIGURED`; lỗi upload → 502.

## 8. Acceptance Criteria (Given-When-Then)
- [ ] **AC-01:** Given email mới, When Admin tạo Staff, Then tài khoản Staff Active + trả initialPassword nếu tự sinh.
- [ ] **AC-02:** Given email đã dùng, When tạo, Then 409.
- [ ] **AC-03:** Given 1000 member, When Staff tìm theo SĐT một phần, Then trả kết quả phân trang đúng < 1s.
- [ ] **AC-04:** Given Member đăng nhập, When xem hồ sơ người khác, Then 403.
- [ ] **AC-05:** Given Admin xóa Member, When truy vấn lại, Then không hiện trong danh sách nhưng vẫn còn trong DB (IsDeleted=1).
- [ ] **AC-06:** Given Staff tạo Member, When hoàn tất, Then có AuditLog `CREATE_MEMBER`.
- [ ] **AC-07:** Given Admin đổi `role` của user, When lưu, Then 422 `ROLE_TRANSITION_NOT_ALLOWED`.
- [ ] **AC-08:** Given Member chưa có hồ sơ, When gọi `GET /members/me`, Then hệ thống tự tạo MemberProfile và trả về.
- [ ] **AC-09:** Given người dùng upload ảnh jpeg ≤5MB, When `POST /users/me/avatar`, Then `avatarUrl` cập nhật (Cloudinary).
- [ ] **AC-10:** Given Admin tạo PT một bước, When hoàn tất, Then có tài khoản role PT + TrainerProfile + initialPassword.

## 9. Out of Scope
- Import hàng loạt Excel/CSV, KYC xác minh danh tính, đăng ký công khai (thuộc spec 001).
- Xoá cứng tài khoản; đổi role (chính sách bất biến).
